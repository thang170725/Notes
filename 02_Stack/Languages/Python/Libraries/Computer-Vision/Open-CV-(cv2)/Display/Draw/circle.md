# .circle()
```bash
cv2.circle(
   img=frame, 
   center=(x, y), 
   radius=5, 
   color=(0, 0, 255), 
   thickness=-1
   lineType,
   shift
)

- Thickness:     Độ dày đường viền. Nếu để số âm (ví dụ: -1), hình tròn sẽ được tô đặc. 2 hoặc -1
```