```bash
SqlAlchemy/
├── 01_schema_definition.md   # Gom: Table, Columns, Meta, base.md (Cấu trúc bảng)
├── 02_core_operations.md     # Gom: Core, engine, execute, fetchall, insert (Thao tác thuần)
└── 03_orm_mode.md            # Gom: orm.md (Cách dùng hướng đối tượng - Session, Model)
```
# Cài đặt
```bash
1. pip install sqlalchemy
2. pip install pymysql
```