# max() & .max() & min() & .min()
Dùng để tìm số lớn nhất và số bé nhất trong mảng numpy.
```python
import numpy as np

a = np.array([1,2,3,4])
print(a.max()) # 4
print(np.max(a)) # 4
# cách sử dụng tương tự cho min và .min()
```