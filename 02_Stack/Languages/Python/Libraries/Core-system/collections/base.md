```text
Sử dụng khi:
- Đếm số lượng, gom nhóm, xử lý danh sách nhanh.
- Tạo dictionary tự động có giá trị mặc định ⇒ không bị lỗi KeyError.
- Tạo queue hoặc stack tối ưu, pop ở đầu không bị O(n) như list.
```