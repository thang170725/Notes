```bash
- Dùng để khớp lệnh cấu trúc (Structural Pattern Matching) và hai là khớp chuỗi (Regex).
- Nó không trả về True/False trực tiếp. Nếu khớp: Trả về một Match Object (đối tượng khớp).Nếu không khớp: Trả về None.
```