# .split()
```bash
Để tách chuỗi và gán thành một mảng.
```
**Syn**
```bash
str.split(separator, maxsplit)

**Ex**
```python
a = "Helllo world"

b = a.split(" ") # b = a.split()
print(b) # ['Helllo', 'world']
```

# .rsplit() & .lsplit()